# kubernetes/applications/traefik.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: traefik
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-5"
spec:
  project: default
  source:
    repoURL: https://traefik.github.io/charts
    chart: traefik
    targetRevision: 37.0.0
    helm:
      releaseName: traefik
      valuesObject:
        deployment:
          kind: DaemonSet
        
        updateStrategy:
          type: OnDelete

        hostNetwork: true
        dnsPolicy: ClusterFirstWithHostNet
        
        securityContext:
          capabilities:
            add:
            - NET_BIND_SERVICE
          privileged: true
          allowPrivilegeEscalation: true
          runAsUser: 0
          runAsGroup: 0

        ports:
          web:
            hostPort: 80
            port: 80
          websecure:
            hostPort: 443
            port: 443
            tls:
              enabled: true
        
        providers:
          kubernetesCRD:
            enabled: true
            allowEmptyServices: true 
        
        rbac:
          enabled: true
        
        tolerations:
          - key: "node-role.kubernetes.io/control-plane"
            operator: "Exists"
            effect: "NoSchedule"
          - key: "node-role.kubernetes.io/master"
            operator: "Exists"
            effect: "NoSchedule"
        
        service:
          enabled: false

  destination:
    server: 'https://kubernetes.default.svc'
    namespace: traefik
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    # The managedNamespaceMetadata block is removed as PSS is now handled globally by K3s.